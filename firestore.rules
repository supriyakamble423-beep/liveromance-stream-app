
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY FULL PUBLIC ACCESS FOR DEBUGGING
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
